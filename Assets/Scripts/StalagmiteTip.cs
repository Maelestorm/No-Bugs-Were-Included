using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StalagmiteTip : MonoBehaviour
{
    //bunu yapacaðý tek þey, player ile trigger anýnda KENDÝSÝNÝ yokedicek. BÝR DE AÞAÐIYA BÝR public static float  VER, BUNUN ADI tipAttackDamage olsun. Bunu player hp scripti içerisindenm çekip hasar anýnda canýmýzý azaltýcaz
    // Start is called before the first frame update

    public static float tipAttackDamage;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
